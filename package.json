{
  "name": "vercel-smtp-sender",
  "version": "1.0.0",
  "description": "Sends email via user SMTP using Nodemailer on Vercel",
  "main": "api/send.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^6.9.7"
  }
}
